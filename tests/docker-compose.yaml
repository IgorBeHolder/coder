version: '3.8'

services:
  transmitter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transmitter_container
    ports:
      - "2222:22"
    networks:
      - llm-net
    restart: unless-stopped

networks:
  llm-net:
    driver: bridge
